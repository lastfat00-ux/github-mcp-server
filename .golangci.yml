run:
  concurrency: 4
  tests: true

linters:
  disable-all: true
  enable:
    - bodyclose
    - gocritic
    - gosec
    - makezero
    - misspell
    - nakedret
    - revive
    - errcheck
    - staticcheck
    - govet
    - ineffassign
    - unused

issues:
  exclude-generated: true
  exclude-rules:
    - path: (third_party|builtin|examples)
      linters:
        - staticcheck
        - govet
        - errcheck
        - revive

linters-settings:
  staticcheck:
    checks:
      - "all"
      - "-QF1008"
      - "-ST1000"
